'use strict';

const IP = '127.0.0.1';
const PORT = '9002';

class Ws
{
    static wsConnection = null;

    static getInstance()
    {
        if (self.wsConnection === undefined || !(self.wsConnection instanceof Ws)) {

        }
    }
}